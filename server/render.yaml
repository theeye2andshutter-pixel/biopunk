services:
  - type: web
    name: haters-neural-server
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENAI_API_KEY
        scope: build
        sync: false
      - key: NODE_ENV
        value: production
